Orange has been installed.

Run the setup helper to configure GDM, dconf, AccountsService,
PAM, and enable the required runit services:

  # orange-setup

Then reboot. Select "Orange" from the GDM session menu.
