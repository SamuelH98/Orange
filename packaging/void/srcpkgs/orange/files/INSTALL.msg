Orange has been installed.

All configuration (dconf, GDM, PAM, runit services) is handled by
the package automatically. Simply reboot to start GDM with Orange:

  # reboot

Select "Orange" from the GDM session menu.
